<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/btnSalvarCadastro"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/edtNomeTurma"
        android:layout_centerVertical="true"
        android:onClick="cadTurma"
        android:text="Salvar" />

    <EditText
        android:id="@+id/edtNomeTurma"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnSalvarCadastro"
        android:layout_alignLeft="@+id/txvTurmaCadastro"
        android:layout_marginBottom="63dp"
        android:ems="10" >

        <requestFocus />
    </EditText>

    <TextView
        android:id="@+id/txvTurmaCadastro"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/edtNomeTurma"
        android:layout_alignParentLeft="true"
        android:layout_marginBottom="41dp"
        android:textSize="40dp"
        android:text="Turma:" />

</RelativeLayout>